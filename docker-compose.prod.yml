version: '3'
services:
  nginx:
    ports:
    - "80:80"
  node-app:
    node-app:
    build: 
      context: .
      args:
        NODE_ENV: production
    environment:
      - NODE_ENV=production
    # env_file:
      # - ./.env
    command: node index.js
  

  